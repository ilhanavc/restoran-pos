export const version = '0000_baseline_legacy_schema';
export const name = 'Legacy schema baseline';

export function up() {
  // Baseline marker only. The existing idempotent schema in run.js remains authoritative.
}
