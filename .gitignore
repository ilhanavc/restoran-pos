node_modules/

client/node_modules/

server/node_modules/

store-bridge/node_modules/

postorancursor/

client/dist/

dist/

dist-electron/

release/

build/

coverage/

logs/

*.log

# Yerel ortam (örnek dosyalar commit edilir: scripts/local-env.example.bat)
.env
.env.local
.env.*.local

server/.env

# SQLite veritabanı ve yardımcı dosyalar (server/data tek blok)
server/data/
server/data/pos.db
server/data/*.db-shm
server/data/*.db-wal

# Kök dizinde yerel yedek / deneme verisi
/data/

scripts/local-env.bat

# .NET build output (tools, local helpers)
tools/**/bin/
tools/**/obj/

# Vendor Caller ID SDK: archives + extracted samples (runtime = tools/callerid-sdk-helper + manual cid.dll)
caller_id/

# Visual Studio / MSBuild
*.suo
*.user

# Local scratch / demo
fisornek.txt

# Yerel proje özeti (Word); isteğe bağlı paylaşım
restoran-pos-v3-ozet.docx
.claude/
.tmp/

# Yerel uygulama config (JWT secret içerebilir — asla commit etme)
pos-config.json

# Üretilmiş rapor/roadmap PDF çıktıları (generate-roadmap.cjs çıktısı)
*.pdf

# Playwright test çıktıları
test-results/
